#include <iostream>
#include "stack.hpp"


int main() {

   /* Stack s1;

    for (int i = 1; i < MAX_SIZE; i++) {
        if (!s1.full()) {
            s1.push(i);
            std::cout << i << " has been pushed into the stack." << std::endl;
        } else {
            std::cout << "Sadly the stack is full..." << std::endl;

        }
    }


    string string1=s1.print();
    std::cout << string1 << std::endl;*/


    return 0;
}